apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - subscription.yaml
  - db-credentials-secret.yaml

labels:
  - pairs:
      app.kubernetes.io/name: cnpg
      app.kubernetes.io/part-of: cnpg-cross-site
    includeSelectors: false

